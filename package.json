{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "Personal academic website with blog",
  "scripts": {
    "build:blog": "node blog/build-blog.js",
    "watch:blog": "nodemon --watch _posts -e md --exec 'npm run build:blog'",
    "update:repos": "node github-repos.js"
  },
  "dependencies": {
    "marked": "^11.1.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
